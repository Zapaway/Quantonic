using System;
using System.Linq;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;

using Quantum;

namespace StateMachines.QSM {
    /// <summary>
    /// Handles the abilities that a controllable can do.
    /// </summary>
    public sealed class QSM : StateMachine<QSMState> {
        // private enum Quantum
        // private ReadOnlyObservableCollection<Qubit> _roQubits;
        // private UnaryQuantumStateDescription? _compositeStateDesc; 

        // public QSM(Controllable controllable, ObservableCollection<Qubit> qubits) {
        //     if (qubits == null) {
        //         throw new ArgumentNullException();
        //     }

        //     _roQubits = new ReadOnlyObservableCollection<Qubit>(qubits);
        // }
    }
}